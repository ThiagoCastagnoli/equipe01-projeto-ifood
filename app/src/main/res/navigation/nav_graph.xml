<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loginFragment">

    <fragment
        android:id="@+id/loginFragment"
        android:name="com.example.equipe01_projeto_ifood.FragmentLogin"
        android:label="FragmentLogin"
        tools:layout="@layout/login_fragment">
        <action
            android:id="@+id/action_fragmentLogin_to_fragmentHomePage"
            app:destination="@id/fragmentHomePage" />
    </fragment>
    <fragment
        android:id="@+id/fragmentHomePage"
        android:name="com.example.equipe01_projeto_ifood.FragmentHomePage"
        android:label="FragmentHomePage"
        tools:layout="@layout/homepage_fragment"/>


</navigation>